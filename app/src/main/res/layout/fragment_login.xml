<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/fragment_login"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    tools:context="com.hackthenorth.lockmeout.app.LoginFragment">

    <RelativeLayout
        android:layout_width="300dp"
        android:layout_height="180dp"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:id="@+id/relativeLayout">

        <EditText
            android:layout_width="250dp"
            android:layout_height="32dp"
            android:inputType="textEmailAddress"
            android:textColor="@android:color/black"
            android:ems="10"
            android:hint="E-mail Address"
            android:id="@+id/email_input"
            android:layout_centerHorizontal="true"
            android:layout_alignParentTop="true"
            android:layout_marginTop="20dp"
            android:layout_marginBottom="20dp"/>

        <EditText
            android:layout_width="250dp"
            android:layout_height="32dp"
            android:inputType="textPassword"
            android:textColor="@android:color/black"
            android:ems="10"
            android:hint="Password"
            android:layout_centerHorizontal="true"
            android:layout_alignTop="@id/email_input"
            android:layout_marginTop="40dp"
            android:id="@+id/passwordInputNew"
             />
        <EditText
            android:layout_width="250dp"
            android:layout_height="32dp"
            android:inputType="textPassword"
            android:textColor="@android:color/black"
            android:ems="10"
            android:hint="Re-type Password"
            android:layout_marginTop="40dp"
            android:layout_centerHorizontal="true"
            android:layout_alignTop="@id/passwordInputNew"
            android:id="@+id/passwordInputNew2"
            />

    </RelativeLayout>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Sign In"
        android:id="@+id/btn_login"
        android:layout_below="@+id/relativeLayout"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
